name: smoke
description: "Quick smoke test - CPU-only models, no GPU required. Use this to verify the framework works."
platform: any
labels:
  - smoke
testCases:
  - opt-125m-cpu
  - cache-aware-sim-cpu
parallel: false
timeout: 15m
